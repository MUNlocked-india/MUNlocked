import StubPage from "@/components/StubPage";

export default function ResearchPage() {
  return (
    <StubPage
      title="Free Research Library"
      blurb="Upload, verification queue, watermarking, ratings, and search are the next subsystem to build for real, using the same Supabase Storage + RLS pattern as conferences."
    />
  );
}
